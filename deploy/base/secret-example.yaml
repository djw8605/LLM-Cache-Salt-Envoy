# Example Secret – DO NOT commit real values.
# Create the real secret with kubectl:
#
#   kubectl create secret generic cache-salt-hmac \
#     --namespace llm-cache-salt \
#     --from-literal=secret="$(openssl rand -base64 32)"
#
# Or use Sealed Secrets / External Secrets Operator in production.
apiVersion: v1
kind: Secret
metadata:
  name: cache-salt-hmac
  namespace: llm-cache-salt
  labels:
    app.kubernetes.io/name: cache-salt-extproc
    app.kubernetes.io/component: extproc
type: Opaque
stringData:
  # Replace with your actual secret value (base64-encoded or plain text via
  # stringData).  This placeholder will cause the pod to start with an unsafe
  # fallback; set a real value before deploying to production.
  secret: "REPLACE_ME_WITH_A_STRONG_RANDOM_SECRET"
